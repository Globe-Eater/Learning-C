#include <stdio.h>
#include <stdlib.h>

int main()
{
	int count = 5;
	double pie = 3.14;
	printf(pie);
	float magnum = 3.57;
	printf(magnum);
	char letter = 'K';
	printf(letter);
	char name[] = "Kellen";
	printf('His name is %s', name);

	return 0;
}
